{
  "rules": {
    "aa14fdd9": {
      "users": {
        "$uid": {
          ".read": "auth != null && $uid === auth.uid",
          ".write": "auth != null && $uid === auth.uid"
        }
      }
    },
    "b6d6cc89": {
      "slogs": {
        "$usertoken": {
          ".read": false,
          ".write": true
        }
      }
    },
    "ce471190": {
      "chats": {
        "$chatroomid": {
          // TODO: fix: check if rules are working in the expected manner
          // ".validate": "$chatroomid.split(':u1:u2:')[0] < $chatroomid.split(':u1:u2:')[1]",
          ".read": true, // "auth != null && $chatroomid.contains(auth.uid)",
          ".write": true //"auth != null && $chatroomid.contains(auth.uid)"
        }
      }
    },
  }
}
